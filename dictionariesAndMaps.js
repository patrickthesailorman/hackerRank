function processData(input) {
    var phoneBook = {
        name: String,
        number: Number
    }
   input.split(" ");
    for (var i = 0; i < input.length; i++) {
        var entries = input[0];
    }
    console.log(input);
} 
